# Docker Environment Configuration for TypeScript Graphzep

# Required: OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_BASE_URL=https://api.openai.com/v1  # Optional: Custom OpenAI endpoint
# MODEL_NAME=gpt-4  # Optional: Override default model
# EMBEDDING_MODEL_NAME=text-embedding-3-small  # Optional: Override embedding model

# Required: Neo4j Database Configuration
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_secure_password_here
NEO4J_PORT=7687  # Default Neo4j Bolt port

# Optional: Server Configuration
# PORT=3000  # Main server port (default: 3000)
# MCP_PORT=3001  # MCP server port (default: 3001)

# Optional: Performance Configuration
# SEMAPHORE_LIMIT=10  # MCP server concurrency limit

# Optional: Development Configuration
# NODE_ENV=production  # Set to development for verbose logging